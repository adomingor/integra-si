<div class='isi_centrar-horiz'>
    <div id='isi_msjPag' class=''>
        {% for label, flashes in app.session.flashbag.all %}
        {% for flash in flashes %}
        <div class='mdl-color-text--{{label}}'>
            <h6>{{ flash | raw }}</h6>
        </div>
        {% endfor %}
        {% endfor %}
    </div>

    <div id='isi_msjProcesando' class='mdl-spinner mdl-js-spinner is-active isi_ocultar'></div>

    <!-- mensajes toast/snack -->
    <div id='isi_msjToastSnack' aria-live='assertive' aria-atomic='true' aria-relevant='text' class='mdl-snackbar mdl-js-snackbar'>
        <div class='mdl-snackbar__text'></div>
        <button type='button' class='mdl-snackbar__action'></button>
    </div>
</div>
